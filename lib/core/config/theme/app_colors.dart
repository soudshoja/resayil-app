import 'package:flutter/material.dart';

/// WhatsApp Native dark theme colors (Design 2)
class AppColors {
  AppColors._();

  // Primary backgrounds
  static const background = Color(0xFF111b21);
  static const surface = Color(0xFF1f2c34);
  static const chatBg = Color(0xFF0b141a);

  // Chat bubbles
  static const outgoingBubble = Color(0xFF005c4b);
  static const incomingBubble = Color(0xFF1f2c34);

  // Accent
  static const accent = Color(0xFF00a884);
  static const accentDark = Color(0xFF008069);

  // Text
  static const textPrimary = Color(0xFFe9edef);
  static const textSecondary = Color(0xFF8696a0);

  // Status indicators
  static const readTicks = Color(0xFF53bdeb);
  static const badge = Color(0xFF00a884);
  static const online = Color(0xFF00a884);
  static const error = Color(0xFFea4335);

  // Divider / borders
  static const divider = Color(0xFF233138);
  static const border = Color(0xFF2a3942);

  // Input
  static const inputBg = Color(0xFF1f2c34);
  static const inputHint = Color(0xFF8696a0);

  // Tab / App bar
  static const appBar = Color(0xFF1f2c34);
  static const tabIndicator = Color(0xFF00a884);
  static const tabSelected = Color(0xFF00a884);
  static const tabUnselected = Color(0xFF8696a0);

  // FAB
  static const fab = Color(0xFF00a884);
}
